
Steps to Run:

Leave backend running and open new terminal 

From rag-frontend/:

In src/api.ts, temporarily use local backend:

const API_URL = "http://127.0.0.1:8000";


npm install -D tailwindcss@3.4.17 postcss autoprefixer

npm install

npm run dev


Build (Vercel)
npm run build